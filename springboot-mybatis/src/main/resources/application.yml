server:
  port: 8088

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://39.106.63.189:3360/mybatis?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: top.guoyuhang.springbootmybatis.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
